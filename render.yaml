services:
  - type: web
    name: gaysplit-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bill_splitter_bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: DATALAB_API_KEY
        sync: false
